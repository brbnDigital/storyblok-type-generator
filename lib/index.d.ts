declare type GeneratorOptions = {
    outputFile?: string;
    namespace?: string;
};
export declare function GenerateTypes(accessToken: string, spaceID: string, options?: GeneratorOptions): Promise<never>;
export {};
